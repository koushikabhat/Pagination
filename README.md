# 🎨 React Data Table with Server-Side Pagination (PrimeReact)

This project is a **React + TypeScript** frontend application built using **PrimeReact**.  
It displays data from the **Art Institute of Chicago API** with manual **server-side pagination**, **row selection**, and a **custom overlay panel** for multi-select input.

---

## 🚀 Features

✅ **Server-side pagination** – Fetches data page by page from the public Art API  
✅ **PrimeReact DataTable** – Clean, responsive, and modern UI  
✅ **Row selection** – Select multiple rows with checkboxes  
✅ **OverlayPanel** – Select rows dynamically by count  
✅ **Loading state** – Displays a loading button/spinner while fetching data  
✅ **TypeScript ready** – Uses `.tsx` and typed hooks for better reliability  
✅ **Responsive design** – Looks great on all screen sizes  

---

## 🧩 Tech Stack

| Library / Tool | Purpose |
|----------------|----------|
| **React** | UI Framework |
| **TypeScript** | Type Safety |
| **PrimeReact** | DataTable, OverlayPanel, Button, Theme |
| **PrimeIcons** | Icons (loading spinner, dropdowns, etc.) |
| **CSS (App.css)** | Custom styling |
| **Art Institute of Chicago API** | Data source |

---

## 📦 Installation & Setup

### 1️⃣ Clone the repository
```bash
git clone https://github.com/your-username/react-datatable-server-pagination.git
cd react-datatable-server-pagination
